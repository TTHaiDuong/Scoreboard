import { forwardRef, useState } from "react"

const availableIds = new Set()

const RoomInfor = forwardRef(() => {
    const [id, setId] = useState<number>(1)


    return (
        <div className="flex">

        </div>
    )
})